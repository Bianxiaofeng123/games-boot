<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eht.y1da.mybatis.mapper.ActivityMapper">

	<select id="joinedActivity" parameterType="java.util.Map" resultType="java.lang.Boolean">
		select count(1)
		from y1da_activity
		<where>
			actid = #{actid}
			and uopid = #{uopid}
			and type= #{type}
		</where>
	</select>
	
	<insert id="joinActivity" parameterType="java.util.Map">
		insert into y1da_activity(actid, actname, uopid, type)
		value(#{actid}, #{actname}, #{uopid}, #{type});
	</insert>

</mapper>